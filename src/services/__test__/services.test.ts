import ImageProcessor from '../image'

describe('Services', () => {
	it('resize should be exist', () => {
		expect(ImageProcessor.resize).toBeDefined()
	})
	it('toFile should be exist', () => {
		expect(ImageProcessor.resize).toBeDefined()
	})
})
